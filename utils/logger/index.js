const logError = require('./error');
const logMessage = require('./log');
const logWarning = require('./warn');
const logSuccess = require('./success');

module.exports = {
  logError,
  logMessage,
  logWarning,
  logSuccess,
};
